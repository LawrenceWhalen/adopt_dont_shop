<%= form_with url: "/applications/#{@application.id}", method: :patch, local: true do |f| %>
  <%= f.label :description, 'Describe why you would make a good pet parent:' %><br>
  <%= f.text_area :description %>
  <%= f.submit "Submit Application" %>
<% end %>
